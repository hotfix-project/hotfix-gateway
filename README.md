# hotfix-gateway
hotfix gateway

# rest api
* GET /check_update?app_id={id}&version={version}
  * request
    * id: int
    * version: string
  * response
* GET /report_update?patch_id={id}
  * request
    * id: int
  * response
